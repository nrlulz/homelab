pvc:
  pvcName: pocket-id-data
  storageRequest: 128Mi
  moverSecurityContext:
    runAsUser: 1000
    runAsGroup: 1000

global:
  createDefaultServiceAccount: false

app-template:
  defaultPodOptions:
    securityContext:
      runAsNonRoot: true
      runAsUser: 1000
      runAsGroup: 1000
      fsGroup: 1000
  controllers:
    pocket-id:
      annotations:
        reloader.stakater.com/auto: "true"
      containers:
        pocket-id:
          image:
            repository: ghcr.io/pocket-id/pocket-id
            tag: v2.16.0-distroless@sha256:f5ce1e58392f48a8fb8a1ff22a714272a49a52b8582d87deb530823f95359204
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          env:
            APP_URL: "https://auth.neils.house"
            TRUST_PROXY: "true"
          envFrom:
            - secretRef:
                name: pocket-id
          probes:
            liveness: &probe
              enabled: true
              custom: true
              spec:
                exec:
                  command:
                    - /app/pocket-id
                    - healthcheck
                initialDelaySeconds: 10
                timeoutSeconds: 5
            readiness: *probe
  service:
    pocket-id:
      controller: pocket-id
      ports:
        http:
          port: 1411
  ingress:
    pocket-id:
      className: external
      hosts:
        - host: auth.neils.house
          paths:
            - path: /
              service:
                identifier: pocket-id
                port: http
  persistence:
    data:
      existingClaim: pocket-id-data
      globalMounts:
        - path: /app/data
