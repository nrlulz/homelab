apiVersion: upgrade.cattle.io/v1
kind: Plan
metadata:
  name: talos
spec:
  # renovate: depName=ghcr.io/siderolabs/installer datasource=docker
  version: v1.12.3
  concurrency: 1
  postCompleteDelay: 2m
  exclusive: true
  serviceAccountName: system-upgrade-controller
  secrets:
    - name: system-upgrade-controller
      path: /var/run/secrets/talos.dev
      ignoreUpdates: true
  nodeSelector:
    matchExpressions:
      - key: feature.node.kubernetes.io/system-os_release.ID
        operator: In
        values:
          - "talos"
  prepare:
    # wait for all attached longhorn volumes to have healthy status
    # some of them take a while to rebuild and we don't wanna interrupt that
    image: ghcr.io/nrlulz/kubectl:v1.35.2@sha256:a2da6573ece966924784b8e489a0f894059cf143ee9a454c2555873bf77d6b16
    command:
      - /bin/sh
    args:
      - -c
      - |
        until [ -z "$(kubectl -n longhorn get volumes.longhorn.io -o json | jq -r ".items[] | select(.status.state==\"attached\" and .status.robustness!=\"healthy\")" )" ]; do
          echo "some volumes are not healthy, waiting..." && sleep 10;
        done
        echo "all volumes are healthy!"
  upgrade:
    image: ghcr.io/jfroy/tnu:0.4.4@sha256:b3f75c7bab94daff74b048410665fffed0a2278e0071ef88e0cf24ba29d48cfc
    envs:
      - name: NODE_IP
        valueFrom:
          fieldRef:
            fieldPath: status.hostIP
    args:
      - --node=$(NODE_IP)
      - --tag=$(SYSTEM_UPGRADE_PLAN_LATEST_VERSION)
