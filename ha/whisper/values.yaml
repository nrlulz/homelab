global:
  createDefaultServiceAccount: false

app-template:
  defaultPodOptions:
    securityContext:
      runAsNonRoot: true
      runAsUser: 1000
      runAsGroup: 1000
  controllers:
    whisper:
      containers:
        whisper:
          image:
            repository: docker.io/rhasspy/wyoming-whisper
            tag: 3.7.0@sha256:ea74afa3e3c347cd031d946b4950b7438ae4bfd07ef96a04ac8a7095f36f4ac6
          args:
            - --language
            - en
            - --model
            - base-int8
            - --data-dir
            - /data
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            seccompProfile:
              type: RuntimeDefault
          probes:
            startup:
              enabled: true
            liveness:
              enabled: true
            readiness:
              enabled: true
  service:
    whisper:
      controller: whisper
      ports:
        http:
          port: 10300
  persistence:
    data:
      type: emptyDir
    dotcache:
      type: emptyDir
      globalMounts:
        - path: /.cache
    tmp:
      type: emptyDir
