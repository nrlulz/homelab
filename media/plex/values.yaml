pvc:
  pvcName: plex-data
  storageRequest: 96Gi
  cacheCapacity: 8Gi
  moverSecurityContext:
    runAsUser: 1000
    runAsGroup: 1000
    fsGroup: 1000

global:
  createDefaultServiceAccount: false

app-template:
  defaultPodOptions:
    securityContext:
      fsGroup: 1000
      fsGroupChangePolicy: OnRootMismatch
  controllers:
    plex:
      containers:
        plex:
          image:
            repository: docker.io/plexinc/pms-docker
            tag: 1.43.3.10896-cb3ebc72d-amd64@sha256:c708587e4874617961a1bc24db9cffa2413653ff422f1b05dfec013339e6824d
          env:
            ADVERTISE_IP: "https://plex.neils.house:443,http://192.168.1.28:32400"
            PLEX_UID: "1000"
            PLEX_GID: "1000"
            TZ: "America/Los_Angeles"
          resources:
            requests:
              gpu.intel.com/i915: 1
            limits:
              gpu.intel.com/i915: 1
              memory: 16Gi
  service:
    plex:
      controller: plex
      type: LoadBalancer
      externalTrafficPolicy: Local
      annotations:
        metallb.io/loadBalancerIPs: 192.168.1.28
      ports:
        http:
          port: 32400
  ingress:
    plex:
      annotations:
        nginx.ingress.kubernetes.io/backend-protocol: HTTPS
      className: external
      hosts:
        - host: plex.neils.house
          paths:
            - path: /
              service:
                identifier: plex
                port: http
  persistence:
    cache:
      storageClass: longhorn-ephemeral
      suffix: cache
      size: 8Gi
      accessMode: ReadWriteOnce
      globalMounts:
        - path: /config/Library/Application Support/Plex Media Server/Cache
    data:
      existingClaim: plex-data
      globalMounts:
        - path: /config/Library/Application Support/Plex Media Server
    logs:
      type: emptyDir
      globalMounts:
        - path: /config/Library/Application Support/Plex Media Server/Logs
    media:
      type: nfs
      server: nas.internal
      path: /mnt/media
      globalMounts:
        - path: /mnt/media
          readOnly: true
    music:
      type: nfs
      server: nas.internal
      path: /mnt/data/music
      globalMounts:
        - path: /mnt/data/music
          readOnly: true
    tmp:
      type: emptyDir
    transcode:
      type: emptyDir
